# 下一步行动计划（W3-W4）

---

## 一、阶段总览

| 周次 | 日期 | 主题 | 关键产出 |
|---|---|---|---|
| W3 | 7/13 - 7/19 | 训练 pipeline + Demo 数据集 + E2E | 真实训练跑通 + Demo 数据 + 联调截图 |
| W4 | 7/20 - 8/02 | 系统测试 + 性能 + 模型对比 | 6 张系统插图 + 测试报告 |
| **冻结** | **8/05** | **代码冻结** | Git tag v1.0.0 |

---

## 二、W3 任务（7/13 - 7/19）✅ 已完成

### 2.1 训练 Pipeline 完善

- [x] **真实进度更新**：每 100 batch 调用 `self.update_state()`（`ml/train.py` line 171）
- [x] **Redis 训练曲线持久化**：每 epoch 写入 `train:history:{task_id}`（`workers/tasks.py` line 79）
- [x] **训练异常处理**：清理 `ModelVersion` + 磁盘 `.pth` + `TrainingJob` 置 FAILED（`workers/tasks.py` line 132）
- [x] **模型版本激活原子化**：`with_for_update()` 行锁（`api/model.py` line 67）
- [x] **超参数配置**：`warmup_epochs` + `early_stop_patience`（`ml/train.py` line 47-48）

### 2.2 Demo 数据集准备

- [x] 写 `scripts/prepare_demo_data.py` 下载 CIFAR-10
- [x] 写 8:2 划分 train/val
- [x] 上传到 `demo/data/cifar10_subset/`（不入 Git）
- [ ] 创建 1 个数据集并导入到系统（需手动执行）

### 2.3 E2E 真实测试

- [x] `tests/e2e_run.py` 10 步流程驱动
- [x] `tests/collect_screenshots.py` Playwright 截图
- [ ] 真实跑通 + 截图（需手动执行）

### 2.4 集成问题修复

- [x] MySQL 真实部署（Docker Compose）
- [x] MinIO 真实部署
- [x] Celery worker 真实启动并验证任务接收
- [x] 跨域配置（Vite 代理 + 后端 CORS）
- [x] 文件上传大小限制（默认 20MB）
- [x] 大数据集分页性能

---

## 三、W4 任务（7/20 - 8/02）

### 3.1 系统测试

#### 功能测试

- [ ] 编写 `tests/test_auth.py`（5 条用例：注册 / 登录 / me / 登出 / 权限）
- [ ] 编写 `tests/test_datasets.py`（4 条用例：CRUD + 类别）
- [ ] 编写 `tests/test_images.py`（3 条用例：上传 / 去重 / 自动标注）
- [ ] 编写 `tests/test_annotations.py`（3 条用例：保存 / 确认 / 修正）
- [ ] 编写 `tests/test_training.py`（2 条用例：启动 / 进度查询）

#### 性能测试

- [ ] 并发上传：50 张同时上传，统计总耗时
- [ ] 批量推理：1000 张图片推理时延（CPU / GPU 对比）
- [ ] 训练时延：5 类 × 200 张 × 5 epoch 总耗时
- [ ] 统计接口响应时延

#### 模型对比实验

**对比矩阵**（在 CIFAR-10 子集上）：

| 模型 | 参数量 | 训练时间 | 准确率 | 精确率 | 召回率 | F1 |
|---|---|---|---|---|---|---|
| ResNet50 | 25M | 待测 | 待测 | 待测 | 待测 | 待测 |
| EfficientNet-B0 | 5M | 待测 | 待测 | 待测 | 待测 | 待测 |
| ConvNeXt-Tiny | 28M | 待测 | 待测 | 待测 | 待测 | 待测 |
| **结论** | | | | | | 选定 |

- [ ] 写 `tests/model_compare.py` 自动跑 3 个模型
- [ ] 写 `tests/results/model_comparison.csv` 导出结果
- [ ] 写对比分析（1500 字）到 `docs/draft/figures/模型对比实验.md`

### 3.2 截图采集（6 张系统插图）

存到 `docs/draft/figures/`：

| 编号 | 名称 | 来源页面 | 用途 |
|---|---|---|---|
| 图 3-1 | 系统架构图 | 手工绘制 | 对应章节 |
| 图 3-2 | 功能模块图 | 手工绘制 | 对应章节 |
| 图 4-1 | 系统主界面 | Layout.vue | 对应章节 |
| 图 4-2 | AI 预标注 Top-5 候选 | Annotate.vue | 对应章节 |
| 图 4-3 | 训练曲线对比 | Training.vue | 对应章节 |
| 图 4-4 | 模型对比结果 | Models.vue | 对应章节 |
| 图 4-5 | 数据集统计仪表盘 | Dashboard.vue | 对应章节 |
| 图 4-6 | 导出 CSV 样例 | Excel 截图 | 对应章节 |

- [ ] 按规范命名 `fig-X-Y-描述.png`，720p 或 1080p
- [ ] 每张图配 100-200 文字说明

### 3.3 测试报告

- [ ] 写 `docs/系统测试报告.md`（含 5 条功能测试用例的执行结果）
- [ ] 性能测试报告（2000 字）作为系统对应章节末"系统测试"小节素材
- [ ] 模型对比实验报告（1500 字）

---

## 四、风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| CPU 训练太慢 | W3 完不成 | 改用 GPU / 减少 epoch / 减小数据集 |
| MySQL 部署问题 | W3 卡住 | 暂时用 SQLite，文档里说"生产用 MySQL" |
| Celery worker 起不来 | 训练任务无法异步 | 改为同步 + 进度轮询 |
| E2E 流程卡在某环节 | 截图不全 | 找最简化的路径，每步单独验证 |
| 模型准确率太低 | 对比实验无意义 | 加大训练轮次 / 调优超参数 |

---

## 五、交付物清单（W3+W4 收官）

### 5.1 代码

- [ ] `tests/` 目录下 5+ 个测试文件
- [ ] `tests/results/` 目录下 CSV 实验数据
- [ ] `tests/e2e_log.md` 完整 E2E 流程记录
- [ ] Git tag `v1.0.0`（2026-08-05）

### 5.2 文档

- [ ] `docs/系统测试报告.md`
- [ ] `docs/性能测试报告.md`
- [ ] `docs/模型对比实验.md`
- [ ] `docs/03-实现开发规划/5-代码冻结检查清单.md`

### 5.3 系统素材

- [ ] 6 张图（PNG）到 `docs/draft/figures/`
- [ ] 3 段实验文字（2000 + 1500 + 1000 字）
- [ ] 训练 + 测试原始数据 CSV

### 5.4 系统评审准备（8 月中下旬开始）

- [ ] PPT（10 分钟现场审查）
- [ ] 演示脚本
- [ ] 备用截图（防止现场网络问题）

---

## 六、状态更新日志

| 日期 | 状态 | 备注 |
|---|---|---|
| 2026-07-11 | 计划制定 | W2 完成，规划 W3-W4 |
| 2026-07-13 | W3 启动 | 训练 pipeline 完善 |
| 待续 | | |
