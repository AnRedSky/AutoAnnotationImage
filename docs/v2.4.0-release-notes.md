# v2.4.0 Release Notes

> **发布日期**: 2026-07-21
> **配套 Commit**: `1f6cd14` (S11.5)
> **覆盖范围**: S10 (v2.3.0) + S11 (v2.3.1 ~ v2.4.0-s11.5)
> **配套文档**: [16-S10收尾报告与S11计划.md](16-S10收尾报告与S11计划.md)

---

## 一、版本亮点

### 🎯 目标检测任务完整化 (S10)

- 检测任务**画布操作 + 全部相关操作**统一到右侧面板 8 sections
- 切图自动保存 (autoSaveBeforeSwitch)
- 画布缩放 (滚轮 + 100% 控制条, 0.25x-8x)
- 画完 bbox 自动选中, 立即可改类别
- AI 预训练模型 (yolov8n/s/m/l/x) 一键启动 (走 ultralytics 预训练权重)

### 🛠️ 代码质量收尾 (S11.5)

- 7 个 Pydantic UserWarning 全部收尾 (从 4 → 0)
- 前端 `npm run build` 0 错误
- 后端 11 个 S10+S11 测试全 PASSED

---

## 二、按 Tag 划分的变更

### v2.3.0 — 检测布局重构

- 左侧画布 + 右侧 6 个 section (工具/类别/选中属性/历史/复制建议/导航)
- 顶部参数区按 task_type 分派

### v2.3.1 — 检测操作全部统一到右侧

- 增加 2 个 section (当前 bbox 列表 / 提交)
- DetectionAnnotator 极简版, 779 → 682 行

### v2.3.1-patch1 — detAnnotRef 声明

- 补 v2.3.1 漏掉的 ref/computed 定义

### v2.3.1-patch2 — catName ref.value

- 修 `categories.find` 缺 .value 错误

### v2.3.1-patch3 — currentTaskType 引用

- 任务类型徽章用 `getTaskTypeMeta(currentTaskTypeRaw)` 代替

### v2.3.2 — detection 5 大功能

1. 标注后保存
2. AI 预训练模型下拉 (yolov8n/s/m/l/x)
3. 切图自动保存
4. 画布缩放
5. 画完可改类别

### v2.3.2-patch1 — resetInitial 函数

- 补 v2.3.2 漏掉的函数定义

### v2.3.2-patch2 — ZoomIn/ZoomOut import

- 补 v2.3.2 漏掉的 icon import

### v2.3.2-patch3 — 保存按钮 disabled 计算

- `:disabled` 表达式改用 `detAnnotRef` 避免 computed 兜底错

### v2.3.2-patch4 — onSave 函数

- 补 v2.3.2 漏掉的 onSave 函数 (emit save)

### v2.3.2-patch5 — TS 错误修复

- 删除重复 onSave
- 修 5 处 TS 类型错误 (`selectedModelId` nullable, `categories.value.find`, `removeBBoxAt`, `datasets` 变量名)

### v2.3.2-patch6 — .gitignore *.pt

- 误提交的 yolov8n.pt 从 git 移除
- 加 .gitignore 条目

### v2.4.0-s11.5 — Pydantic 收尾

- `PreviewConfidenceRequest` (image.py:32) 加 `model_config = ConfigDict(protected_namespaces=())`
- 测试覆盖扩展到 6 个 schema (含此新增)

---

## 三、与论文配套

| 论文章节 | 用到的素材 |
|---|---|
| 第 3 章 系统设计 | 任务类型拓展 (v2.0.0), 检测布局 (v2.3.x) |
| 第 4.1 章 主界面 | Annotate.vue 截图 (检测/分类/分割三态) |
| 第 4.2 章 数据管理 | DatasetDetail.vue 截图 |
| 第 4.5 章 训练 | Training.vue 截图 + 曲线 |
| 第 4.6 章 测试 | 11 个 pytest + 0 个 Pydantic warning |
| 第 5 章 性能 | 论文插图 (待 S11.2 采集) |

---

## 四、累计 Tags (v2.0.0 ~ v2.4.0)

| Tag | 主题 | Commit |
|---|---|---|
| v2.0.0 | 任务类型拓展基础设施 | - |
| v2.1.0 | Detection/Segmentation 标注组件 | 93c3762 |
| v2.1.1 | 依赖修复 (移除 pycocotools) | f767710 |
| v2.1.2 | MySQL 缺失列迁移脚本 | 1293a69 |
| v2.1.3 | MySQL 存储过程迁移 | 733360e |
| v2.1.4 | init_db 自动迁移 | ac2359c |
| v2.1.5 | Vite `export {}` 修复 | fb57231 |
| v2.1.6 | 数据集列表 task_type 修复 | e611106 |
| v2.1.7 | Pydantic 4 文件修复 | 8f17234 |
| v2.1.8 | clearBBoxes 路由修复 | 4eed31e |
| v2.2.0 ~ v2.2.3 | S9 检测画布/统计/复制/训练可视化 | - |
| v2.3.0 | 检测布局重构 | be0e1bc |
| v2.3.1 + 3 patch | 检测操作统一右侧 | 995d012 |
| v2.3.2 + 6 patch | detection 完整功能 + 收尾 | 05592b4 |
| **v2.4.0-s11.5** | **Pydantic 收尾 (此版本)** | **1f6cd14** |

---

## 五、向后兼容性

- ✅ 已有数据 (v1.0.0 ~ v2.3.2) 无破坏
- ✅ MySQL 自动迁移 (v2.1.4 init_db)
- ✅ 前端 build 0 错误
- ✅ 后端 11 个测试 PASSED
- ⚠️ 论文数据集 task_type 仍以 v1.0.0 旧值 ('classification') 存储

---

## 六、待办 (S11.x)

- [ ] S11.1 性能基准 (100 张检测图推理时延)
- [ ] S11.2 论文 6 张插图
- [ ] S11.3 分割任务布局统一 (像 S10 检测)
- [ ] S11.4 训练可视化按 task_type 切换

**完整冻结时间**: 2026-08-05 → Git tag `v2.4.0`
