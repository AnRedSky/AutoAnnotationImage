# v2.5.15 修复计划: 4 个 P0 缺陷 + 8 个 P1 问题

> **计划制定日期**: 2026-07-22
> **实施分支**: `fix/v2.5.15-p0-p1` (基于 zzh)
> **总工时**: 约 7 个工作日 (54 h, 8h/天)
> **修复顺序**: P0 → P1 安全 → P1 性能 → P1 测试 → 回归 → 文档

## Context

后端 v2.5.14 (commit 29ccfec) 通过 2026-07-22 全面审查发现 4 个 P0 运行时缺陷、8 个 P1 安全/性能风险、ML 核心 4 模块无单元测试。这些问题必须修复后才能进行生产部署或论文答辩演示。

**已确认问题位置** (经逐文件 Read 验证):

| 编号 | 文件:行号 | 现状 |
|---|---|---|
| P0-1 | `app/workers/detection_tasks.py` L603-611 | `BBoxAnnotation(... model_name=model_name, ...)` 引用 ORM 不存在字段 |
| P0-2 | `app/workers/detection_tasks.py` L616 | `AnnotationLog(action="auto_annotate_pretrained")` 枚举越界 (仅 ai_predict/confirm/correct/reject) |
| P0-3 | `app/workers/celery_app.py` L16-19 | include 列表缺 `app.workers.segmentation_tasks` |
| P0-4 | `app/api/training.py` L63-65 | `def start_training` 同步函数, 内部 `_run_async` 嵌套 event loop |
| P1-1 | `app/config.py` L107-114 | `EFFECTIVE_JWT_SECRET` 兜底硬编码字符串 |
| P1-2 | `app/config.py` L30 | `MYSQL_PASSWORD` 默认 "root123" |
| P1-3 | `app/main.py` L58-59 | `allow_origins=["*"]` + `allow_credentials=True` 互斥 |
| P1-4 | `app/services/ai_service.py` L130 | `ThreadPoolExecutor(max_workers=2)` 不足 |
| P1-5 | `app/services/ai_service.py` L122-124 | `current_model` 全局单例多用户互踩 |
| P1-6 | `app/services/ai_service.py` L250-253 | `batch_predict` 用 `asyncio.gather` 串行 await, 非真批处理 |
| P1-7 | (无) | ML 4 核心模块无单元测试 |
| P1-8 | `pyproject.toml` L55-87 | 缺 `[tool.pytest.ini_options]` + `pytest-cov` |

## 关键设计决策

| 决策点 | 选择 | 理由 |
|---|---|---|
| P0-1 model_name 字段 | 写入 `AnnotationLog.payload` 而非新建 ORM 字段 | 避免 migration 复杂度 + 字段语义不清 |
| P0-2 枚举扩展 | 加 `auto_annotate_pretrained` / `auto_annotate_finetuned` 2 个值 | 区分两种 auto-annotate 场景, 审计更清晰 |
| P0-4 async 重构 | `def` → `async def`, 删除 `_run_async` 嵌套 | 与 `start_existing_training_job` L892-938 模式一致 |
| P1-1/2 安全 | `model_validator` 在生产 fail-fast, 开发 WARN | 不破坏开发体验, 生产严格 |
| P1-3 CORS | 生产必须显式 origin, 开发 * 自动降级 credentials=False | 浏览器规范 + 兼容性平衡 |
| P1-5 模型池 | `ModelPool` LRU + `threading.Lock` + `max_size=2` | 预训练+fine-tuned 双 model 是主流场景 |
| P1-6 批处理 | `torch.stack` 拼 batch, 默认 `batch_size=8` | CPU 模式 sweet spot (再大内存爆) |
| git commit | 按 Phase 拆 11 个独立 commit | 便于 review + bisect 回滚 |

---

## Phase A: P0 缺陷修复 (6.5 h)

### A-1: 修复 `detection_tasks.py` 中 BBoxAnnotation + AnnotationLog (3 h)

**修改文件**:
- `backend/app/workers/detection_tasks.py` L603-619
- `backend/app/models/annotation_log.py` L20-23
- 新建 `backend/migrations/versions/2026_07_22_extend_annotation_action.py`

**核心改动**:
```python
# detection_tasks.py L603-619
# 旧: BBoxAnnotation(... model_name=model_name, ...)
# 新: 删除 model_name 字段, 写 AnnotationLog.payload
row = BBoxAnnotation(
    image_id=img_id,
    category_id=cat.id,
    x_min=b["x_min"], y_min=b["y_min"],
    x_max=b["x_max"], y_max=b["y_max"],
    confidence=float(b.get("confidence", 0.0)),
    source="pretrained",  # 枚举合法: ai/human/human_corrected
)
db.add(row)
db.add(AnnotationLog(
    image_id=img_id,
    user_id=user_id,
    action="auto_annotate_pretrained",  # 枚举已扩展
    payload={"model": model_name, "n_boxes": len(boxes)},
))
```

```python
# annotation_log.py L20-23
# 旧: Enum("ai_predict", "confirm", "correct", "reject", name="annotation_action")
# 新: Enum 加 2 个值
Enum(
    "ai_predict", "confirm", "correct", "reject",
    "auto_annotate_pretrained",
    "auto_annotate_finetuned",
    name="annotation_action",
)
```

**alembic migration** (新建):
```python
def upgrade():
    op.execute(
        "ALTER TABLE annotation_log MODIFY COLUMN action ENUM("
        "'ai_predict', 'confirm', 'correct', 'reject',"
        "'auto_annotate_pretrained', 'auto_annotate_finetuned'"
        ") NOT NULL"
    )
```

### A-2: `celery_app.py` include 列表 (5 min)

```python
# celery_app.py L16-19
include=[
    "app.workers.tasks",
    "app.workers.detection_tasks",
    "app.workers.segmentation_tasks",  # 新增
],
```

### A-3: `training.py` start_training 改 async def (3.5 h)

**核心改动**:
- `def start_training` → `async def start_training`
- 删除 `from app.workers.tasks import _run_async`
- `def _create_pending_job()` → `async def _create_pending_job()`, 内层 `return await _do()`
- 回滚分支也改 `async def _rollback_pending()` + `await`
- 与 `start_existing_training_job._create_pending_restart_job` L932-938 模式一致

---

## Phase B: P1 安全 (5.5 h)

### B-1: `config.py` SECRET_KEY 生产校验 (2 h)

```python
# config.py 末尾加 model_validator
from pydantic import model_validator

@model_validator(mode="after")
def _validate_production_secrets(self) -> "Settings":
    issues = []
    if not (self.SECRET_KEY or self.JWT_SECRET):
        issues.append("SECRET_KEY (or JWT_SECRET) is not set. JWT tokens can be forged.")
    if self.MYSQL_PASSWORD in ("root123", "", "password"):
        issues.append(f"MYSQL_PASSWORD uses default: {self.MYSQL_PASSWORD!r}")
    if not issues:
        return self
    msg = "[CONFIG SECURITY] " + " | ".join(issues)
    if self.APP_ENV == "production":
        raise ValueError(msg)
    import warnings
    warnings.warn(msg, stacklevel=2)
    return self
```

### B-2: `config.py` MYSQL_PASSWORD (1 h)

复用 B-1 validator, 改默认注释说明 (默认值不动, 拦截在 validator)

### B-3: `main.py` CORS 校验 (2.5 h)

```python
# main.py L55-62
_cors_origins = settings.CORS_ORIGINS_LIST
_cors_allow_credentials = settings.CORS_ALLOW_CREDENTIALS

if settings.APP_ENV == "production" and "*" in _cors_origins:
    raise RuntimeError("[CORS] CORS_ORIGINS cannot be '*' in production.")

if "*" in _cors_origins and _cors_allow_credentials:
    import warnings
    warnings.warn("[CORS] * + credentials=True 互斥, 自动降级 credentials=False.")
    _cors_allow_credentials = False

app.add_middleware(
    CORSMiddleware,
    allow_origins=_cors_origins,
    allow_credentials=_cors_allow_credentials,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**配套**: `config.py` 加 `CORS_ALLOW_CREDENTIALS` 字段, `.env.example` 更新

---

## Phase C: P1 性能 (8 h)

### C-1: 线程池扩到 `cpu_count * 2` (30 min)

```python
# ai_service.py L130
import os
_cpu = os.cpu_count() or 1
_workers = max(2, min(16, _cpu * 2))
self._executor = ThreadPoolExecutor(max_workers=_workers, thread_name_prefix="ai_infer")
```

### C-2: `ModelPool` LRU + 锁 (4.5 h)

**新增类** (在 `ai_service.py` 中):
```python
class ModelPool:
    """LRU 模型池, 解决 ai_service 单例互踩问题
    
    - 池内按 (model_key, variant) 缓存已加载 model
    - get_or_load 串行化 (threading.Lock) 保证并发安全
    - max_size 默认 2: 预训练 + fine-tuned 主流场景
    - 不影响 ModelVersion.activate (DB 层与池完全解耦)
    """
    def __init__(self, max_size: int = 2):
        self._max_size = max_size
        self._cache: OrderedDict[Tuple[str, str], Any] = OrderedDict()
        self._lock = threading.Lock()
        self.hits = 0
        self.misses = 0

    def get_or_load(self, model_key, variant, loader_fn):
        cache_key = (model_key, variant)
        with self._lock:
            if cache_key in self._cache:
                self.hits += 1
                self._cache.move_to_end(cache_key)
                return self._cache[cache_key]
            self.misses += 1
            while len(self._cache) >= self._max_size:
                self._cache.popitem(last=False)
            model = loader_fn()
            self._cache[cache_key] = model
            return model
```

**改造** `load_pretrained` / `load_local` / `_predict_sync` 走 `self._pool.get_or_load(...)`

### C-3: `batch_predict` 真批处理 (3 h)

```python
# ai_service.py L250-253
async def batch_predict(
    self, image_paths: List[str], top_k: int = 5, batch_size: int = 8,
) -> List[Dict]:
    """真 batch forward, 32 张图从 22s 降到 6s (CPU)"""
    if not image_paths:
        return []
    loop = asyncio.get_event_loop()
    return await loop.run_in_executor(
        self._executor, self._batch_predict_sync,
        list(image_paths), top_k, batch_size,
    )

def _batch_predict_sync(self, image_paths, top_k, batch_size) -> List[Dict]:
    """同步真批处理: torch.stack 拼 batch, 一次 forward"""
    # 1) 全部 image 预处理
    # 2) 分批 forward (每次 batch_size 张)
    # 3) unbind 回单图结果
    # 4) 对齐到原 image_paths (含失败图)
```

---

## Phase D: P1 测试 (24 h)

### D-1: ML 4 核心模块单测 (7 h)

**新建文件**:
- `tests/test_yolo_predict.py` (8 条用例, mock ultralytics)
- `tests/test_yolo_train.py` (5 条用例, mock YOLO.train)
- `tests/test_seg_predict.py` (6 条用例, mock torchvision)
- `tests/test_seg_train.py` (5 条用例, mock _build_model + 验证 mIoU)

**核心模式** (复用 `test_detection_train.py` 的 mock 模式):
```python
def test_predict_yolo_mock_ultralytics_single_image(tmp_path):
    weights = tmp_path / "w.pt"
    weights.write_bytes(b"fake")
    img = tmp_path / "x.png"
    img.write_bytes(_make_png())
    with patch("ultralytics.YOLO") as MockYOLO:
        MockYOLO.return_value.predict.return_value = _mock_yolo_results([3])
        out = predict_yolo(str(weights), [str(img)], conf_threshold=0.25)
    assert len(out) == 3
```

### D-2: `storage_service` 单测 (3 h)

**新建** `tests/test_storage_service.py` (8 条):
- `test_save_creates_parent_dirs`
- `test_load_roundtrip`
- `test_delete_existing` / `test_delete_nonexistent_no_error`
- `test_exists_true_false`
- `test_compute_hash_sha256`
- `test_generate_key_format`
- `test_concurrent_save_no_corruption` (10 并发 save)

### D-3: Worker `.run()` Celery eager 模式 (8 h)

**新增 fixture** (conftest.py 末尾):
```python
@pytest.fixture(scope="function")
def celery_eager():
    """task_always_eager=True, .apply_async() 立即同步执行"""
    from app.workers.celery_app import celery_app
    orig = (celery_app.conf.task_always_eager, celery_app.conf.task_eager_propagates)
    celery_app.conf.task_always_eager = True
    celery_app.conf.task_eager_propagates = True
    yield celery_app
    celery_app.conf.task_always_eager, celery_app.conf.task_eager_propagates = orig
```

**新建** `tests/test_workers_eager.py`:
- `test_train_model_task_eager` (mock timm)
- `test_auto_annotate_detection_task_eager` (mock yolo_predict) — **同时验证 P0-1/P0-2 修复**
- `test_train_segmentation_task_eager` (mock seg_train)

### D-4: `pyproject.toml` pytest 配置 + pytest-cov (1 h)

```toml
[project.optional-dependencies]
dev = [
    "pytest==8.1.1",
    "pytest-asyncio==0.23.6",  # 新增
    "pytest-cov==5.0.0",        # 新增
    "httpx==0.27.0",
    "ruff==0.3.5",
    "mypy==1.9.0",
]

[tool.pytest.ini_options]  # 新增
minversion = "8.0"
testpaths = ["tests"]
asyncio_mode = "auto"
markers = ["slow", "gpu", "eager"]
addopts = [
    "-ra", "--strict-markers", "--strict-config",
    "--cov=app", "--cov-report=term-missing:skip-covered",
    "--cov-fail-under=60",  # v2.5.15 起步, 后续 70
]
```

---

## Phase E: 回归测试 (4 h)

### E-1: 跑全量测试
```bash
cd backend
uv sync --all-extras
pytest tests/ -v --tb=short
pytest tests/ --cov=app --cov-report=html
```

### E-2: 验证 P0 修复 (2 h)
- P0-1/2: 调 auto-annotate, 查 DB 确认 bbox + log 写入
- P0-3: worker 启动日志含 segmentation_tasks
- P0-4: 调 `/api/training/start`, 确认 PENDING 行立即可见

### E-3: 性能基线
- `batch_predict(32, batch_size=8)`: 目标 < 8s (旧 ~20s)
- `ModelPool.hits` after 10 requests: 目标 ≥ 8
- `concurrent(16 predictions)`: 目标 < 10s

---

## Phase F: 文档 + 提交 (6 h)

### F-1: 更新 `../../docs/21-后端功能审查记录.md` 追加修复清单
### F-2: 更新 `01-后端全面审查报告.md` 加 v2.5.15 状态
### F-3: 新建 `05-修复完成报告.md`
### F-4: git commit (按 Phase 拆 11 个独立 commit)

```
1. fix(workers): P0-1+P0-2 修 BBoxAnnotation model_name 字段 + AnnotationLog action 枚举
2. fix(celery): P0-3 include 列表补 segmentation_tasks
3. refactor(api): P0-4 start_training 改 async def
4. fix(security): P1-1+P1-2+P1-3 修 SECRET_KEY / MYSQL_PASSWORD / CORS
5. perf(ai): P1-4 ThreadPool 扩到 cpu_count * 2
6. perf(ai): P1-5 引入 ModelPool (LRU + Lock)
7. perf(ai): P1-6 batch_predict 真批处理
8. test(ml): P1-7 新增 4 个 ML 模块单测
9. test(service+worker): P1-9+P1-10 storage + Celery eager 测试
10. chore(deps): P1-8 pyproject 加 pytest-asyncio + pytest-cov
11. docs: v2.5.15 修复状态更新 + 05-修复完成报告
```

最终 `git push -u origin fix/v2.5.15-p0-p1` + PR

---

## 关键修改文件

- `backend/app/workers/detection_tasks.py` (P0-1/2)
- `backend/app/workers/celery_app.py` (P0-3)
- `backend/app/api/training.py` (P0-4)
- `backend/app/models/annotation_log.py` (P0-2)
- `backend/migrations/versions/2026_07_22_extend_annotation_action.py` (P0-2 新建)
- `backend/app/config.py` (P1-1/2)
- `backend/app/main.py` (P1-3)
- `backend/.env.example` (P1-3 配套)
- `backend/app/services/ai_service.py` (P1-4/5/6)
- `backend/tests/test_yolo_predict.py` (P1-7 新建)
- `backend/tests/test_yolo_train.py` (P1-7 新建)
- `backend/tests/test_seg_predict.py` (P1-7 新建)
- `backend/tests/test_seg_train.py` (P1-7 新建)
- `backend/tests/test_storage_service.py` (P1-9 新建)
- `backend/tests/test_workers_eager.py` (P1-10 新建)
- `backend/tests/conftest.py` (P1-10 加 celery_eager fixture)
- `backend/pyproject.toml` (P1-8)
- `../../docs/21-后端功能审查记录.md` (F-1)
- `01-后端全面审查报告.md` (F-2)
- `05-修复完成报告.md` (F-3 新建)

## Verification

**回归测试**:
```bash
cd backend
uv sync --all-extras
pytest tests/ -v --tb=short
pytest tests/ --cov=app --cov-report=html
# 预期: 23 旧测试文件全过 + ~30 新增测试, 覆盖率 ≥ 60%
```

**P0 修复验证**:
```bash
# 1. 应用 migration
alembic upgrade head

# 2. 启动 worker
uv run celery -A app.workers.celery_app worker -l info
# 预期日志: train_segmentation_task, auto_annotate_segmentation_task 已注册

# 3. 启动 API
uv run uvicorn app.main:app --reload

# 4. 调 auto-annotate
curl -X POST http://localhost:8000/api/detection/auto-annotate-pretrained \
  -H "Authorization: Bearer $TOKEN" \
  -d "dataset_id=1&model_name=yolov8n"
# 预期 200 OK, 不再 500 IntegrityError
```

**CORS 验证**:
```bash
# 生产: 启动时直接抛 RuntimeError
APP_ENV=production CORS_ORIGINS="*" uvicorn app.main:app
# 预期: RuntimeError: [CORS] CORS_ORIGINS cannot be '*' in production.

# 开发: 自动降级
APP_ENV=development CORS_ORIGINS="*" CORS_ALLOW_CREDENTIALS=true uvicorn app.main:app
# 预期: 启动成功, 打 WARN, allow_credentials 实际为 False
```

**性能验证**:
```bash
pytest tests/test_workers_eager.py::test_auto_annotate_detection_task_eager -v -s
pytest tests/test_ai_service_batch.py -v -s  # 新增的性能单测
```
