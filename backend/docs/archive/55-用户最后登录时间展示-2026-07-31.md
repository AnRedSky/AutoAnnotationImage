# 用户管理最后登录时间 — 2026-07-31

## 改动

### 后端

**User model** (`app/admin/model/user.py`)
  - 新增字段: `last_login_at: Mapped[datetime | None]`
  - 同步加: `updated_at: Mapped[datetime] = onupdate=datetime.utcnow`

**Migration** (`app/database/migration.py`)
  - 新增 `_USER_TABLE_NEW_COLUMNS` 常量 (last_login_at, updated_at)
  - 新增 `_ensure_user_new_columns()` helper — 用 `information_schema.COLUMNS` 幂等检查后 `ALTER TABLE ... ADD COLUMN`
  - 挂到 `ensure_v2_0_0_schema()` 主入口

**auth_service.py** — `AuthService.login` 成功路径:
  ```python
  user.last_login_at = datetime.utcnow()
  await db.commit()
  ```

**user.py** — `/users/` 列表 + `/me/profile` 响应中新增字段:
  ```python
  "last_login_at": u.last_login_at.isoformat() if u.last_login_at else None,
  ```

### 前端

**Users.vue**
  - `UserItem` interface 加 `last_login_at: string | null`
  - 表格新增"最后登录"列 (`width=170`)
    - 显示格式: 刚刚 / N 分钟前 / N 小时前 / N 天前 / 日期 (tooltip 完整时间)
    - 未登录: 灰色斜体"从未登录"
  - 新增 `fmtRelative()` 函数

**admin.css**
  - 新增 `.never-logged` 样式 (灰色斜体 12px)

## 验证

| 检查 | 结果 |
|---|---|
| DB migration: `last_login_at` + `updated_at` 已加 | ✅ SHOW COLUMNS 确认 |
| `/api/auth/login` 写入 last_login_at | ✅ admin=2026-07-31T06:15:32 |
| `/api/users/` 返回 last_login_at | ✅ 7 用户, admin 有值, 其他 None |
| `/api/users/me/profile` 返回 last_login_at | ✅ admin 有值 |
| vue-tsc | ✅ 0 error |
| backend healthcheck | ✅ 186 files OK |

## 数据库 DDL

```sql
ALTER TABLE `user` ADD COLUMN `last_login_at` DATETIME NULL;
ALTER TABLE `user` ADD COLUMN `updated_at`    DATETIME NULL;
```
