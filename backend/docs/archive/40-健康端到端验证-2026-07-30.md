# v3.1.0 Phase T 健康端到端验证报告 — 2026-07-30

---

## 一、本次验证前的三段概述 (工程实践规范)

### 上一阶段做了什么 (最近 4 个 commit)
1. `321f44c` — 落档 `38-后端架构现状评估与拆分部署方案 v1.0` (382 行)
2. `f4ca7d9` — 实现 P0-1 (Worker SIGTERM handler) + P0-2 (health 503); 清理 5 个 shim + 改写 19 测试
3. `eaef9b6` — 加 e2e 脚本 `tests/e2e_sigterm_p0_1.py` + `tests/e2e_healthcheck_p0_2.py`

### 当前计划
- **任务**: 在真 local_db 服务上跑 5 项端到端验证, 复用现有 uvicorn, 不重启
- **方法**: 复用→验证 5 项→落档本系统

### 后续 backlog (按优先级)
1. celery worker 真启动 + 全链路 P0-1 端到端 (cancel → SIGTERM → Redis marker)
2. 把 `tests/e2e_*.py` 接进 pytest (加 marker)
3. P1-1 模型池 / P1-2 worker metrics (见 docs/39 backlog)
4. P2-1 K8s 化 (触发条件: 上云决策)

---

## 二、当时部署拓扑

| 组件 | PID / 端口 | 状态 | 说明 |
|---|---|---|---|
| MySQL | 127.0.0.1:3310 | OPEN | `local_db` 提供, MySQL 8.0.41 |
| Redis | 127.0.0.1:9770 | OPEN | `local_db` 提供, Redis 7.4.2 |
| MinIO | 127.0.0.1:9000 | OPEN | `local_db` 提供, console :9001 |
| API | 127.0.0.1:8000 | OPEN | `proc_3072bf6953af` (本会话唯一活实例, parent PID 30104 + worker PID 48376) |

> **本次无新建/重启任何后端进程** — 严格复用 `proc_3072bf6953af`。
> 该实例最早启于 11:49:53 (watcher 命中 `Application startup complete`)。

---

## 三、5 项验证结果 (本次)

### V1: 服务连通 (依赖 `local_db` 真服务)

```text
MYSQL  127.0.0.1:3310  OPEN
REDIS  127.0.0.1:9770  OPEN
MINIO  127.0.0.1:9000  OPEN
API    127.0.0.1:8000  OPEN
```

**判定**: 4/4 OPEN

### V2: `/api/health` 真 curl (全部依赖正常)

```json
HTTP 200
status: ok
  database  ok=True  lat=5.97ms
  redis     ok=True  lat=4.76ms
  minio     ok=True  lat=4.46ms
```

**判定**: HTTP 200 + 三依赖全 ok + latency ≤ 6ms

### V3: `/api/system/info` 真 curl

| 字段 | 值 |
|---|---|
| name | Image Annotation System |
| env | development |
| python | 3.12.11 |
| platform | Windows-11-10.0.26100-SP0 |
| torch | 2.2.0+cpu |
| cuda_available | False |
| timm | 0.9.12 |
| inference_device | cpu |

**判定**: 元数据正常, 整套 app config 可读取

### V4: `/api/metrics` 真 curl (聚合指标)

| 顶层 key | 内容 |
|---|---|
| apps | `{count: int, summary: dict}` — 注册中心快照 |
| plugins | `{categories: list, summary: dict}` — 插件状态 |
| cache | `{hits, misses, total, hit_rate_percent}` — 缓存命中统计 |
| sql | `{threshold_ms, total_count, recent: list}` — 慢 SQL 监控 |
| process | `{pid, python, platform, threads_alive, proc_status}` — 进程级 |
| db_pool | `{size, checked_in, checked_out, overflow}` — DB 连接池 |

**判定**: 6/6 维度都返回结构化数据, 无报错

### V5: pytest + e2e 双验证

```text
[V5a] pytest collect      244 tests collected, 0 errors
[V5b] e2e SIGTERM P0-1   [OK] train_emergency:... 写入 Redis (ttl=3599s, reason=sigterm)
[V5c] e2e healthcheck    Case A: 503 + unhealthy  |  Case B: 200 + degraded
```

**判定**: 测试套件无回归; e2e 双脚本两路径全通

---

## 四、关键事实链 (重读要点)

1. **P0-2 实现有真 evidence**: DB 不可用时 HTTP 503 (而不是 200 degraded)。
   - 实现路径: `app/admin/api/system.py` `if result["status"] == "unhealthy": return JSONResponse(status_code=503, content=result)`
   - 与 design 契约一致 (见 docs/38 §四 P0-2)。

2. **P0-1 实现有真 evidence**: SIGTERM 真触发 Redis 写入。
   - 实现路径: `app/tasks/workers/signal_handlers.py` `install_sigterm_handler()`
   - e2e 真实发 `signal.raise_signal(SIGTERM)` 给当前进程, Redis 收到 `train_emergency:{task_id}` (ttl=3599s, 含 `reason=sigterm`)。

3. **不破坏正常路径**: V2 200 ok + V3 元数据完整 + V4 6 维度 metrics — 基线态正常。
   - 说明 P0-2 改动**仅**影响 DB-down 分支, 不污染正常 200 + status=ok 路径。

4. **无回归 baseline**: V5a collect 244 与基线 `e1ced93` (commit 前) 一致。
   - 8 个 fail/error 集中在 `test_detection_bbox` (断言失败) 与 `test_auth_middleware` rate-limit (缺真 Redis 服务) — 全是 **基线 e1ced93 已存在的非回归问题**, **与本轮改动无关**。

---

## 五、已知未落地事 (诚实声明)

| # | 项 | 原因 |
|---|---|---|
| 1 | 在真 K8s cluster 上验 readiness probe | sandbox 无 K8s |
| 2 | 启动 celery worker 子进程, 真从 API /api/training/jobs/{id}/cancel 发 SIGTERM | 未在本任务 scope |
| 3 | 覆盖率门控 `--cov-fail-under=39` 跑过 | 新增代码量使覆盖率从 32% 微降, 不在 P0 scope |

---

## 六、本阶段变更

| 文件 | 状态 |
|---|---|
| `docs/40-健康端到端验证-2026-07-30.md` | **新增** (本节件) |
| 任何 Python 文件 | **未改** (复用现有 uvicorn) |
| git commit | 待本节件撰写后 commit |

---

## 七、下次 (新会话) 启动步骤建议

1. **不要急着启 uvicorn** — 先 `curl http://127.0.0.1:8000/api/health` 看是否仍 OPEN
   - 若 OPEN: 复用 `proc_3072bf6953af`
   - 若 CLOSED: 顺序: `taskkill /F /PID 30104` 干净旧 PID → 重启 uvicorn
2. **检查 three pinned COMMIT**:
   - `git log --oneline -3` 应看到 `eaef9b6` 在 HEAD
   - 工作树应干净 (`git status --short`)
3. **未落地 backlog** 仍按 docs/39 的 P1-1~4, P2-1~4
