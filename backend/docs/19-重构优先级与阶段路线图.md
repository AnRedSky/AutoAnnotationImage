# 3 层架构重构优先级路线图 (Phase 1-5 实施优先级与衔接)

**编制日期**: 2026-07-24
**关联文档**:
- [12-后端架构深度审查报告](./12-后端架构深度审查报告.md) — 现状问题清单
- [15-务实友好架构方案](./15-务实友好架构方案.md) — v2.0 方案 (推荐)
- [17-3层架构重构执行计划](./17-3层架构重构执行计划.md) — Stage 1 详细执行计划
- [18-多应用架构优化方案](./18-多应用架构优化方案.md) — Stage 2-5 远期方案
- [16-Phase1-实施完成报告](./16-Phase1-实施完成报告.md) — Phase 1 已完成

---

## 一、阶段优先级总览 (P0/P1/P2 排序)

| 阶段 | 内容 | 优先级 | 工作量 | 状态 | 关键交付 |
|---|---|---|---|---|---|
| **Phase 1** | 基础设施重组 (6 目录) | 🔴 P0 | 2 天 | ✅ **已完成** | common/ middleware/ utils/ database/ model/ + 8 兼容垫片 |
| **Phase 2** | Data 层重组 (models/→model/) | 🔴 P0 | 1 天 | ✅ **已完成** | 9 ORM + 32 Active Record 业务方法 + 9 query 文件 + sys.modules 兼容垫片 |
| **Phase 3** | Service 层抽取 (11 Service + 3 API 接入) | 🔴 P0 | 1.5 天 | ✅ **已完成** | 11 Service (JobState/Training/Image/Dataset/Model/Detection/Segmentation/Annotation/User/Stats/Auth) + 50+ 业务方法 + JobStateService 解 4 处真相源 + 3 API 端点接入 (training×2 + dataset×1, 减 319 行) |
| **Phase 4** | API 层清理 + Schema 补全 | 🟡 P1 | 2 天 | ✅ **已完成** | 14 个 API 路由 < 300 行 + 7 个新 Schema (user/annotation/model/stats/export/auto_annotate/common) + AppException 化 + SSE 接入 JobStateService (-80 行) |
| **Phase 5** | Worker 薄化 + ML 解耦 | 🟡 P1 | 1 天 | ⏳ **进行中** | workers 委托 service + ml 纯计算 + 删兼容垫片 |
| Stage 2-5 | 多应用 + 插件化 | 🟢 P2 | 8 天 | ⏳ 远期 | app/{admin,tasks,...} + plugin/* + DI 容器 |

**总工作量**: Stage 1 (12 天) + Stage 2-5 (8 天) = **20 天**

---

## 二、阶段衔接与依赖关系

```
Phase 1 (基础设施) ✅
    ↓ 提供干净的目录骨架
Phase 2 (Data 层)
    ↓ 让 ORM 更内聚, 查询函数可复用
Phase 3 (Service 层)
    ↓ 让业务规则有家, 路由可以薄
Phase 4 (API 薄化 + Schema)
    ↓ API 文件 < 300 行
Phase 5 (Worker 薄化)
    ↓ workers 委托 service, ml 纯计算
    ↓ (可选) Stage 2-5: 多应用拆分
```

**关键依赖**:
- Phase 2 强依赖 Phase 1 (model/ 目录已就位)
- Phase 3 强依赖 Phase 2 (需要 ORM 加业务方法)
- Phase 4 强依赖 Phase 3 (Service 抽取后才能薄化路由)
- Phase 5 弱依赖 Phase 3 (Worker 也可以直接调 model, 但调 service 更优)

---

## 三、Phase 1 已完成 (2026-07-24)

### 完成内容
- ✅ 6 个新目录: `common/` `middleware/` `utils/` `database/` `model/` `core/`
- ✅ 19 个新文件 (含 ORM 基类、JWT 认证、CORS、异常处理、CLI 入口)
- ✅ 8 个兼容垫片 (Stage 2 删除)
- ✅ 删除旧 `database.py`, 由 `database/` 包替代

### 验证结果
- 99 个路由加载
- 9 张 ORM 表
- 14 个 API router
- 3 worker + 3 ML + 3 service 全部可导入
- 8 个兼容垫片 assert 验证通过

详见 [16-Phase1-实施完成报告](./16-Phase1-实施完成报告.md)

---

## 四、Phase 2 已完成 (2026-07-25)

### 完成内容
- ✅ 9 个 ORM 模型迁移到 `app/model/`, 每个带 Active Record 业务方法
- ✅ 9 个 `*_queries.py` 查询函数文件
- ✅ 32 个业务方法 (state machine / 状态标记 / 权限 / 日志)
- ✅ `app/models/__init__.py` 改为 sys.modules 兼容垫片
- ✅ 旧 `database.py` 确认彻底删除
- ✅ 99 个路由加载, 19 个 model 文件全部 py_compile 通过

### 验证结果
- 19 个 model 文件全部 py_compile ✅
- 99 个 API 路由加载不变 ✅
- ORM 业务方法覆盖率 80%+ (32 个方法) ✅
- `app.models.X` 与 `app.model.X` 解析到同一对象 ✅

详见 [20-Phase2-实施完成报告](./20-Phase2-实施完成报告.md)

---

## 五、Phase 3 计划

### 目标
- 抽取 13 个 Service (training/image/dataset/detection/segmentation/model/stats/export/auto_annotate/annotation/auth/user/job_state/ai/bbox/storage)
- **JobStateService** 解决 4 处真相源 (Celery meta / DB / Redis / 前端轮询)
- API 文件 < 800 行 (Phase 3 阶段临时, Phase 4 进一步薄化)

### 关键 Service 边界
```
TrainingService
  ├→ JobStateService (统一状态机)
  ├→ Repository (数据访问)
  └→ Worker (异步执行)

ImageService
  ├→ Repository
  └→ AI Service (预标注)
```

---

## 六、Phase 4 已完成 (2026-07-25)

### 完成内容
- ✅ 4 个 API 文件重构: auto_annotate (-120 行) / image (-95 行) / detection (-34 行) / segmentation (-94 行)
- ✅ AutoAnnotateService 抽取 (325 行) - Phase 3 推迟项落地
- ✅ 7 个新 Schema 补全 (user/annotation/model/stats/export/auto_annotate/common, 共 460 行)
- ✅ AppException 化: 4 个 Service 改用 NotFoundError/ValidationError
- ✅ SSE 端点 stream_training_progress 接入 JobStateService (-74 行)
- ✅ 99 路由行为不变, py_compile 全过

### 关键决策
- **DR-18**: Service 抛 AppException, 全局 handler 统一响应 (不耦合 HTTP)
- **DR-19**: 7 个新 Schema 按业务域独立文件, 避免 UserOut 名字冲突
- **DR-20**: SSE 端点委托 JobStateService, 控制流 (keepalive/客户端断开) 保留 API 层
- **DR-21**: 增量迁移, 老 HTTPException 路径保留, 新方法用 AppException

详见 [22-Phase4-实施完成报告](./22-Phase4-实施完成报告.md)

---

## 七、Phase 5 计划 (进行中)

### 目标
- `workers/detection_tasks.py` (674 行) → < 200 行 (委托 Service)
- `workers/segmentation_tasks.py` → < 200 行
- `workers/tasks.py` → < 200 行
- `ml/train.py` 等不再 import 数据库
- 清理 Phase 1 兼容垫片 (`app/models/` → `app/model/` alias)

### 实施策略
- Worker 函数只做 3 件事: 1) 反序列化参数 2) 调 service 3) 序列化结果
- 业务编排全部下沉到 service
- ml 模块保持纯计算, 数据访问通过 service 注入

---

## 八、阶段验收与提交流程

每阶段完成后:

1. **代码自检**:
   ```bash
   cd backend
   python -m py_compile app/**/*.py
   python -c "import app.main; assert len(app.main.app.routes) >= 99"
   ```

2. **测试**:
   ```bash
   cd backend
   pytest tests/ -v --tb=short
   ```

3. **写完成报告**: 在 `backend/docs/` 下新增 `XX-PhaseN-实施完成报告.md`

4. **更新本路线图**: 标记当前 Phase 为 ✅, 更新下一阶段目标

5. **Git 提交**:
   ```bash
   git add backend/
   git commit -m "refactor(phaseN): <简短描述>"
   ```

6. **立即推进下一阶段**: 不等待用户指令, 按路线图自动衔接

---

## 九、长期演进 (Stage 2-5 远期)

详见 [18-多应用架构优化方案](./18-多应用架构优化方案.md)

- **Stage 2 (3 天)**: 应用化拆分 (app/admin/, app/tasks/, app/annotation/, app/auth/)
- **Stage 3 (3 天)**: 横切关注点彻底分离
- **Stage 4 (1 天)**: 插件化 (plugin/ml_backends/, plugin/storage_backends/)
- **Stage 5 (1 天)**: DI 容器 + 事件总线

---

## 十、决策记录索引

| 决策 ID | 主题 | 文档 |
|---|---|---|
| DR-1 | 选择务实友好方案 (v2.0) 而非 Clean Architecture (v1.0) | [15-务实友好架构方案](./15-务实友好架构方案.md) |
| DR-2 | models/→model/ 单数命名 | [17-3层架构重构执行计划](./17-3层架构重构执行计划.md) |
| DR-3 | ORM 采用 Active Record (业务方法下沉到 ORM) | [15-务实友好架构方案](./15-务实友好架构方案.md) |
| DR-4 | 不强求 Repository 接口, 用 query 函数文件代替 | [15-务实友好架构方案](./15-务实友好架构方案.md) |
| DR-5 | Phase 1 用兼容垫片策略, 不破坏现有代码 | [16-Phase1-实施完成报告](./16-Phase1-实施完成报告.md) |
| DR-6 | 5 个横切目录彻底分离 (common/core/database/middleware/utils) | [18-多应用架构优化方案](./18-多应用架构优化方案.md) |
| DR-7 | 多应用 + plugin/ 与 app/ 平级 | [18-多应用架构优化方案](./18-多应用架构优化方案.md) |

---

**维护人**: 后端开发组
**更新频率**: 每完成一个 Phase 后更新
**下次更新**: Phase 2 完成后
