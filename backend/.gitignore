__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/

# uv
.uv/
# uv.lock 显式保留 (便于可复现构建, API/Worker 镜像构建都依赖此锁文件)
!uv.lock

# 环境
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# 测试
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# 日志
*.log
logs/
# 临时文本日志 (兜底, *.log 已覆盖, 这里显式声明)
backend_log.txt

# Celery 调度文件
celerybeat-schedule
celerybeat-schedule.db

# 异步任务进程管理
worker.pid
beat.pid

# 覆盖率 / 测试报告
coverage.xml
junit*.xml

# 数据库
*.db
*.sqlite3

# 模型与数据 (精确路径, 避免误忽略 backend/app/models/*.py ORM 代码)
/models/*.pth
/models/*.onnx
/models/hf_cache/
uploads/
data/raw/
data/processed/
# 演示/合成数据 (由 scripts/prepare_*_demo.py 生成, 不入版本库)
demo/data/
# 系统
Thumbs.db
