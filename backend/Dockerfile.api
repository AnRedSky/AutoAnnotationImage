# syntax=docker/dockerfile:1.7
# =====================================================================
#  API-only stage — 复用主 Dockerfile 的 base stage
# ---------------------------------------------------------------------
#  使用方法 (buildkit 必需):
#    docker build --target api -t annotation/api:local -f Dockerfile.api .
#  或在 docker-compose 中:
#    build:
#      context: ./backend
#      dockerfile: Dockerfile.api
#      target: api
#  ↑ target 字段是关键
#
#  本文件结构:
#    1) builder stage (与主 Dockerfile 一致, 独立可重复声明)
#    2) base stage   (与主 Dockerfile 一致)
#    3) api stage    (最终目标)
# =====================================================================

# ============== Stage 1: builder ==============
FROM python:3.10-slim AS builder

RUN apt-get update && apt-get install -y --no-install-recommends \
        gcc g++ pkg-config \
    && rm -rf /var/lib/apt/lists/*

COPY --from=ghcr.io/astral-sh/uv:0.5.11 /uv /uvx /usr/local/bin/

WORKDIR /build
COPY pyproject.toml uv.lock ./

ENV UV_LINK_MODE=copy \
    UV_PROJECT_ENVIRONMENT=/app/.venv \
    UV_COMPILE_BYTECODE=1 \
    PIP_NO_CACHE_DIR=1

# v3.3.1 修复: 添加 --no-dev 排除开发依赖 (mypy/ruff/pytest), 节省 1-3 分钟构建时间
RUN uv sync --frozen --no-install-project --no-dev

# ============== Stage 2: base ==============
FROM python:3.10-slim AS base

RUN apt-get update && apt-get install -y --no-install-recommends \
        curl ca-certificates gosu \
    && rm -rf /var/lib/apt/lists/*

COPY --from=ghcr.io/astral-sh/uv:0.5.11 /uv /uvx /usr/local/bin/

# v3.3.2 修复: 先建用户, 再 COPY --chown, 消除 chown -R 翻倍层
RUN groupadd -r app && useradd -r -g app -d /app -s /sbin/nologin app \
    && mkdir -p /app/uploads /app/models /app/logs

COPY --chown=app:app --from=builder /app/.venv /app/.venv

WORKDIR /app
COPY --chown=app:app pyproject.toml uv.lock ./
COPY --chown=app:app app /app/app
COPY --chown=app:app scripts /app/scripts
COPY --chown=app:app migrations /app/migrations

# v3.3.2: 入口脚本 — 修复命名卷权限后通过 gosu 降权到 app 用户
COPY docker-entrypoint.sh /app/docker-entrypoint.sh
# 强制清除windows回车符，兜底
RUN sed -i 's/\r$//g' /app/docker-entrypoint.sh \
    && chmod +x /app/docker-entrypoint.sh
RUN chmod +x /app/docker-entrypoint.sh

ENV PATH="/app/.venv/bin:$PATH" \
    UV_LINK_MODE=copy \
    UV_PROJECT_ENVIRONMENT=/app/.venv \
    PIP_NO_CACHE_DIR=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    APP_PORT=8000

ENTRYPOINT ["/app/docker-entrypoint.sh"]
EXPOSE 8000

# ============== Stage 3: api (final) ==============
FROM base AS api

HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=5 \
    CMD curl -fsS "http://127.0.0.1:8000/api/health" || exit 1

CMD ["uvicorn", "app.main:app", \
     "--host", "0.0.0.0", "--port", "8000", \
     "--proxy-headers", "--forwarded-allow-ips", "*"]
